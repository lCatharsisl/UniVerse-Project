# Environment variables for UniVerse backend

# Server
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=universe
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_MAX_CONNECTIONS=20

<<<<<<< Updated upstream
# Logging
LOG_LEVEL=info
=======
# Supabase Storage (optional — sohbet/görseller tüm cihazlarda aynı URL ile açılır)
# Dashboard → Project Settings → API: Project URL + service_role secret
# Dashboard → Storage → New bucket (ör. "media") → Public bucket veya okuma policy
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=
# Supabase Storage’taki bucket adı (Dashboard’daki ile aynı olmalı, örn. uploads)
# SUPABASE_STORAGE_BUCKET=uploads

# Arama: PostgreSQL tam metin (Supabase/Postgres; migrations/016_search_postgres_and_view.sql uygulayın: pg_trgm, v_user_search, indeksler)

# Yemek listesi PDF (varsayılan: yasar.edu.tr). Sunucu IP’si Cloudflare’de 403 alırsa aynı dosyayı S3/public URL’e koyup burada verin.
# YASAR_MENU_PDF_URL=https://www.yasar.edu.tr/yemek-liste.pdf

# Microsoft login (optional)
# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=
# MICROSOFT_TENANT_ID=
# MICROSOFT_REDIRECT_URI=http://localhost:3000/api/auth/microsoft/callback
>>>>>>> Stashed changes
