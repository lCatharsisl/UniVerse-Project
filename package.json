{
    "name": "universe-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "node scripts/dev.mjs",
        "dev:lan": "DEV_LAN=1 node scripts/dev.mjs",
        "sync:frontend-env": "node scripts/sync-frontend-env.mjs",
        "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "ci:backend": "npm run build --prefix backend && npm test --prefix backend && npm run test:integration --prefix backend",
        "ci:frontend": "npm run lint --prefix frontend && npm run test:smoke --prefix frontend && npm run build --prefix frontend && npm run test:e2e --prefix frontend",
        "ci": "npm run ci:backend && npm run ci:frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "overrides": {
    "lodash": "4.18.1"
  }
}
