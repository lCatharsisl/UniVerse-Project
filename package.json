{
    "name": "universe-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "ci:backend": "npm run build --prefix backend && npm test --prefix backend",
        "ci:frontend": "npm run lint --prefix frontend && npm run build --prefix frontend",
        "ci": "npm run ci:backend && npm run ci:frontend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
